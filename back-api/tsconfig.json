{
  "compilerOptions": {
    /* === 컴파일 경로 설정 === */
    "rootDir": "src", // TypeScript 소스가 들어있는 폴더
    "outDir": "dist", // 컴파일된 JavaScript 파일을 출력할 폴더

    /* === 공통 설정 === */
    "target": "es2016",
    "module": "commonjs",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },


  "include": ["src/**/*"],

  /* 만약 컴파일에서 제외할 파일/폴더가 있다면 여기에 적습니다. */
  "exclude": ["node_modules", "dist"]
}
